{
  "name": "%{project_name}",
  "version": "1.0.0",
  "description": "",
  "main": "./build",
  "author": "Qompium",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "start": "yarn build && node invoke",
    "build": "yarn lint --fix && tsc",
    "test": "yarn build && jest",
    "test-local": "yarn build && jest && node invoke",
    "lint": "eslint src/**/*.ts"
  },
  "dependencies": {
    "@extrahorizon/javascript-sdk": "^3.1.0",
    "axios-logger": "^2.5.0",
    "dotenv-safe": "^8.2.0"
  },
  "devDependencies": {
    "@qompium/eslint-config-q-node": "^1.2.9",
    "@qompium/license-checker": "^1.0.3",
    "@types/dotenv-safe": "^8.1.1",
    "@types/jest": "^26.0.23",
    "@types/node": "12.12.21",
    "eslint": "7.27.0",
    "jest": "27.0.4",
    "jest-junit": "^12.0.0",
    "nock": "^13.0.11",
    "ts-jest": "27.0.3",
    "typescript": "^4.2.4"
  },
  "jest-junit": {
    "outputDirectory": "test-results",
    "outputName": "jest-junit.xml"
  }
}
